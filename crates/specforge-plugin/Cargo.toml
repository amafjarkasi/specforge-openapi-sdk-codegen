[package]
name = "specforge-plugin"
description = "SDK for building specforge WASM emitter plugins"
version.workspace = true
edition.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
